import { NextResponse } from "next/server"; import { db } from "@/lib/db"; import { getSession } from "@/lib/auth";
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){const s=await getSession();if(!s)return NextResponse.json({error:"Unauthorized"},{status:401});const {id}=await params;const b=await db.booking.findUnique({where:{id}});if(!b||b.clientId!==s.userId&&s.role!=="ADMIN")return NextResponse.json({error:"Запись не найдена"},{status:404});const {status}=await req.json();if(!["CANCELLED","CONFIRMED","COMPLETED"].includes(status))return NextResponse.json({error:"Недопустимый статус"},{status:400});return NextResponse.json(await db.booking.update({where:{id},data:{status}}))}
