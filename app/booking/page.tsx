"use client"; import { useState } from "react"; import { toast } from "sonner"; import { CalendarCheck, Check } from "lucide-react";
export default function Booking(){const [done,setDone]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const data=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch("/api/bookings",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(data)});if(r.status===401)return toast.error("Войдите, чтобы записаться");if(!r.ok)return toast.error("Не удалось создать запись");setDone(true)}
if(done)return <section className="container grid min-h-[60vh] place-items-center py-20"><div className="card max-w-lg text-center"><div className="mx-auto grid size-16 place-items-center rounded-full bg-blush text-rose"><Check size={30}/></div><h1 className="mt-5 font-serif text-3xl font-bold">Вы записаны!</h1><p className="mt-3 text-black/50">Подтверждение появится в кабинете. За день до визита мы отправим напоминание.</p></div></section>;
return <section className="container py-16"><div className="mx-auto max-w-2xl"><div className="eyebrow">Онлайн-запись</div><h1 className="mt-2 font-serif text-5xl font-bold">Выберите удобное время</h1><form onSubmit={submit} className="card mt-8 grid gap-5"><label>Услуга<select className="input mt-2" name="service"><option>Маникюр с покрытием · 2 400 ₽</option><option>Комплексный уход · 3 300 ₽</option></select></label><label>Дата<input required className="input mt-2" type="date" name="date"/></label><label>Время<div className="mt-2 grid grid-cols-4 gap-2">{["10:00","12:00","14:30","17:00"].map(x=><label key={x}><input className="peer sr-only" type="radio" name="time" value={x} required/><span className="block cursor-pointer rounded-xl bg-cream p-3 text-center peer-checked:bg-ink peer-checked:text-white">{x}</span></label>)}</div></label><textarea className="input min-h-24" name="comment" placeholder="Комментарий мастеру (необязательно)"/><button className="btn-primary"><CalendarCheck/>Подтвердить запись</button></form></div></section>}
